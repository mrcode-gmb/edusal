volumes:
  nexus_local_postgres_data: {}
  nexus_local_postgres_data_backups: {}
  nexus_local_redis_data: {}

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: nexus_local_django
    container_name: nexus_local_django
    depends_on:
      - postgres
      - redis
      - mailpit
    volumes:
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8001:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: nexus_production_postgres
    container_name: nexus_local_postgres
    volumes:
      - nexus_local_postgres_data:/var/lib/postgresql/data
      - nexus_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  mailpit:
    image: docker.io/axllent/mailpit:latest
    container_name: nexus_local_mailpit
    ports:
      - "8025:8025"

  redis:
    image: docker.io/redis:7.2
    container_name: nexus_local_redis
    volumes:
      - nexus_local_redis_data:/data

  celeryworker:
    <<: *django
    image: nexus_local_celeryworker
    container_name: nexus_local_celeryworker
    depends_on:
      - redis
      - postgres
      - mailpit
    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: nexus_local_celerybeat
    container_name: nexus_local_celerybeat
    depends_on:
      - redis
      - postgres
      - mailpit
    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: nexus_local_flower
    container_name: nexus_local_flower
    ports:
      - '5555:5555'
    command: /start-flower
